{
  "name": "keeperhub-safe-first-write",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A dependency-free safe first-write starter and onboarding audit for KeeperHub builders.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "doctor": "node scripts/doctor.mjs",
    "first-write": "node scripts/first-write.mjs",
    "test": "node --test"
  }
}
